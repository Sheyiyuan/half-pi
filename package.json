{
	"name": "half-pi-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"clean": "npm run clean --workspaces --if-present",
		"build": "cd packages/ai && npm run build && cd ../agent && npm run build && cd ../tui && npm run build && cd ../half-pi && npm run build",
		"check": "true",
		"test": "npm run test --workspaces --if-present"
	},
	"engines": {
		"node": ">=22.19.0"
	}
}
